import { IdToCommentPipe } from './id-to-comment.pipe';

describe('IdToCommentPipe', () => {
  it('create an instance', () => {
    const pipe = new IdToCommentPipe();
    expect(pipe).toBeTruthy();
  });
});
